/**
 * @deprecated Import `AppSelect` from `../ui/AppSelect` for new code.
 * Kept as a compatibility alias for existing settings tabs.
 */
export {
  AppSelect as SettingsSelect,
  type AppSelectOption as SettingsSelectOption,
} from "../ui/AppSelect";
